rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /system/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && firestore.exists(/databases/(default)/documents/admin_list/$(request.auth.uid));
    }
  }
}
